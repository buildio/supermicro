name: Release

# Publishes to RubyGems through trusted publishing (OIDC) - no API key is stored
# in this repository. Tag a release commit as vX.Y.Z and push the tag.
on:
  push:
    tags: ['v*']

permissions:
  contents: write   # create the GitHub release
  id-token: write   # mint the OIDC token RubyGems verifies

jobs:
  release:
    runs-on: ubuntu-latest
    env:
      BUNDLE_FROZEN: "false"
    steps:
      - uses: actions/checkout@v5
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler-cache: true
      - name: Run specs
        run: bundle exec rspec
      # Not rubygems/release-gem: that runs `bundle exec rake release`, and
      # bundler trips over securerandom being both a default gem and a
      # transitive activesupport dependency. Build and push with plain rubygems
      # instead; the credentials come from OIDC either way.
      - name: Configure RubyGems credentials
        uses: rubygems/configure-rubygems-credentials@v2.1.0
      - name: Build and push the gem
        run: |
          gem build *.gemspec
          gem push *.gem
